import { styled } from '../../styles'

export const TextArea = styled('textarea', {
  display: 'flex',

  width: '1038px',
  height: '120px',
  resize: 'none',
  borderRadius: '4px',
  marginTop: '14px',
  borderStyle: 'none',
  boxShadow:
    'rgba(0, 0, 0, 0.05) 0px 4px 20px 8px, rgba(0, 0, 0, 0.025) 0px 0px 0px 2px',
})
